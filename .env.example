# Copy to .env and fill in. .env is gitignored — never commit real keys.
#
# The variable NAMES here must match the `api_key_env` fields in
# config/sqout.yaml. The `light` and `heavy` roles can point at different
# variables if you use different providers for each.

ANTHROPIC_API_KEY=
